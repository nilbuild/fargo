## What

<!-- What does this change? Why? -->

## Testing

<!-- How did you check that it works? If it touches streaming or recording,
     which destinations did you really test with? -->

- [ ] `make build` passes
- [ ] `make test` passes
- [ ] Tested by hand on a real Mac, not only built

## Checklist

- [ ] Follows the code style around it (early exits, braces around `if` bodies, no comments that repeat the code)
- [ ] No secrets in the code (OAuth client IDs and secrets, stream keys)
- [ ] Updated `README.md` if the behavior changed
